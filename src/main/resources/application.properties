# MySQL
jdbc.driver=com.mysql.cj.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/space_knowledge?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
jdbc.username=root
jdbc.password=123456

# Redis
redis.host=localhost
redis.port=6379

# JWT
jwt.secret=space-knowledge-platform-secret-key-change-in-production
jwt.expiration=86400000

# Hikari (add to pom if not present)
# Using HikariCP via spring-jdbc (add hikaricp dependency in pom for explicit use)
