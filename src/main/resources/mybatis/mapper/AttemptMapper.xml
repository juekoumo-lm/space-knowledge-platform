<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.space.knowledge.mapper.AttemptMapper">
    <resultMap id="BaseResultMap" type="Attempt">
        <result column="user_id" property="userId"/>
        <result column="question_id" property="questionId"/>
        <result column="level_id" property="levelId"/>
        <result column="is_correct" property="isCorrect"/>
        <result column="time_spent" property="timeSpent"/>
        <result column="attempt_no" property="attemptNo"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO attempts (user_id, question_id, level_id, answer, is_correct, time_spent, attempt_no, source)
        VALUES (#{userId}, #{questionId}, #{levelId}, #{answer}, #{isCorrect}, #{timeSpent}, #{attemptNo}, #{source})
    </insert>
    <select id="selectByUserId" resultMap="BaseResultMap">SELECT * FROM attempts WHERE user_id=#{userId} ORDER BY created_at DESC LIMIT #{limit}</select>
    <select id="selectRecentByUserId" resultMap="BaseResultMap">SELECT * FROM attempts WHERE user_id=#{userId} ORDER BY created_at DESC LIMIT #{n}</select>
    <select id="countCorrectByUser" resultType="int">SELECT COUNT(*) FROM attempts WHERE user_id=#{userId} AND is_correct=1 AND created_at &gt;= #{since}</select>
    <select id="countByUserSince" resultType="int">SELECT COUNT(*) FROM attempts WHERE user_id=#{userId} AND created_at &gt;= #{since}</select>
</mapper>
