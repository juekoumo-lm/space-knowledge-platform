<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.space.knowledge.mapper.QuestionOptionMapper">
    <resultMap id="BaseResultMap" type="QuestionOption">
        <result column="question_id" property="questionId"/>
        <result column="option_label" property="optionLabel"/>
        <result column="option_text" property="optionText"/>
        <result column="is_correct" property="isCorrect"/>
    </resultMap>
    <select id="selectByQuestionId" resultMap="BaseResultMap">SELECT * FROM question_options WHERE question_id = #{questionId} ORDER BY option_label</select>
    <select id="selectByQuestionIds" resultMap="BaseResultMap">
        SELECT * FROM question_options WHERE question_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY question_id, option_label
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question_options (question_id, option_label, option_text, is_correct)
        VALUES (#{questionId}, #{optionLabel}, #{optionText}, #{isCorrect})
    </insert>
    <insert id="insertBatch">
        INSERT INTO question_options (question_id, option_label, option_text, is_correct) VALUES
        <foreach collection="list" item="o" separator=",">(#{o.questionId}, #{o.optionLabel}, #{o.optionText}, #{o.isCorrect})</foreach>
    </insert>
    <delete id="deleteByQuestionId">DELETE FROM question_options WHERE question_id = #{questionId}</delete>
</mapper>
